<?php

declare(strict_types=1);

// Coordinated release: keep the configured position count as the hard cap while adaptive capacity only scales new BUY sizing and never blocks scanning or forces liquidation below that configured cap.
return '1.4.21';
