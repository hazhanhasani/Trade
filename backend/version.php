<?php

declare(strict_types=1);

// Coordinated release: republishes the completed dashboard truth + no-flash theme debug together with the post-release Repair/Cron diagnostic compatibility fix. Stable assets now come from the current main runtime snapshot so cPanel, Android and manifest stay coordinated.
return '1.4.25';
